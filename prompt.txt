project_root/
│
├── binary_pipeline/          ← your existing code, kept as-is
│   ├── src/
│   └── ...
│
└── multiclass_pipeline/
    ├── data/                 ← same parquet, different label column
    ├── configs/
    │   └── config.py         ← MODE, NUM_CLASSES=5, CLASS_NAMES
    ├── preprocessing/
    │   ├── preprocess.py     ← same cleaning, adds label encoder
    │   └── label_encoder.py  ← LabelEncoder + class weights
    ├── features/
    │   └── features.py       ← same hand features, unchanged
    ├── vectorizers/
    │   ├── ml_vectorizer.py  ← same TF-IDF
    │   ├── bert_vectorizer.py← same tokenizer
    │   └── dl_vectorizer.py  ← same vocab builder
    ├── models/
    │   ├── ml/
    │   │   └── ml_models.py  ← LR/RF/XGB updated for multiclass
    │   ├── dl/
    │   │   └── bilstm.py     ← output layer changed to 5 classes
    │   └── llm/
    │       └── llm_classifier.py ← Gemini/Claude prompt updated
    ├── training/
    │   └── trainer.py        ← class-weighted loss, diagnostics
    ├── evaluation/
    │   └── evaluator.py      ← macro/micro F1, per-class ROC, OvR
    └── utils/
        └── class_weights.py  ← compute_class_weight helper