Step 4 — BERT update. Change num_labels and the problem type.
pythonfrom transformers import AutoModelForSequenceClassification

model = AutoModelForSequenceClassification.from_pretrained(
    'bert-base-uncased',
    num_labels=5,                          # <-- was 1 or 2
    problem_type='single_label_classification'
)

# In training loop, use integer labels 0-4, not binary
# Loss is computed internally by HuggingFace when labels are passed
outputs = model(input_ids=ids, attention_mask=mask, labels=y_batch)
loss = outputs.loss   # automatically CrossEntropy